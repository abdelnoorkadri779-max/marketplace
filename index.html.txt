<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
  <meta charset="UTF-8" />
  <title>سوق إلكتروني</title>
  <meta name="viewport" content="width=device-width, initial-scale=1" />

  <!-- Firebase SDK -->
  <script src="https://www.gstatic.com/firebasejs/9.23.0/firebase-app-compat.js"></script>
  <script src="https://www.gstatic.com/firebasejs/9.23.0/firebase-auth-compat.js"></script>
  <script src="https://www.gstatic.com/firebasejs/9.23.0/firebase-firestore-compat.js"></script>
  <script src="https://www.gstatic.com/firebasejs/9.23.0/firebase-storage-compat.js"></script>

  <style>
    * {
      box-sizing: border-box;
    }
    
    body {
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      background: #000;
      color: white;
      margin: 0;
      padding: 0;
      direction: rtl;
    }
    
    /* التصميم الرئيسي */
    .header {
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      padding: 20px;
      text-align: center;
      position: fixed;
      top: 0;
      left: 0;
      right: 0;
      z-index: 1000;
      box-shadow: 0 4px 20px rgba(0,0,0,0.3);
    }
    
    .header h1 {
      margin: 0;
      font-size: 28px;
      font-weight: bold;
      background: linear-gradient(45deg, #fff, #f0f0f0);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
    }
    
    .container {
      margin-top: 100px;
      padding: 15px;
    }
    
    /* صناديق الإدخال */
    .card {
      background: rgba(255, 255, 255, 0.1);
      backdrop-filter: blur(10px);
      border-radius: 20px;
      padding: 25px;
      margin-bottom: 20px;
      border: 1px solid rgba(255, 255, 255, 0.2);
      box-shadow: 0 8px 32px rgba(0, 0, 0, 0.2);
    }
    
    .card h3 {
      margin-top: 0;
      color: #4ecdc4;
      font-size: 22px;
      margin-bottom: 20px;
      text-align: center;
    }
    
    /* حقول الإدخال */
    input, textarea, select, button {
      width: 100%;
      padding: 16px;
      margin: 10px 0;
      border-radius: 12px;
      border: 2px solid rgba(255, 255, 255, 0.2);
      font-size: 16px;
      transition: all 0.3s ease;
      background: rgba(255, 255, 255, 0.1);
      color: white;
    }
    
    input:focus, textarea:focus, select:focus {
      outline: none;
      border-color: #4ecdc4;
      box-shadow: 0 0 0 3px rgba(78, 205, 196, 0.3);
    }
    
    input::placeholder, textarea::placeholder {
      color: rgba(255, 255, 255, 0.6);
    }
    
    /* الأزرار */
    button {
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      color: white;
      border: none;
      cursor: pointer;
      font-weight: bold;
      font-size: 18px;
      letter-spacing: 1px;
      transition: transform 0.3s, box-shadow 0.3s;
    }
    
    button:hover {
      transform: translateY(-3px);
      box-shadow: 0 10px 25px rgba(102, 126, 234, 0.4);
    }
    
    button:active {
      transform: translateY(-1px);
    }
    
    /* تصميم الفيديو TikTok */
    .feed {
      height: calc(100vh - 150px);
      overflow-y: scroll;
      scroll-snap-type: y mandatory;
      -webkit-overflow-scrolling: touch;
      scrollbar-width: none;
    }
    
    .feed::-webkit-scrollbar {
      display: none;
    }
    
    .video-item {
      height: calc(100vh - 150px);
      scroll-snap-align: start;
      position: relative;
      display: flex;
      flex-direction: column;
      justify-content: flex-end;
      padding: 25px;
      overflow: hidden;
      background: linear-gradient(135deg, #1a1a2e, #16213e);
    }
    
    .item-image {
      position: absolute;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      object-fit: cover;
      z-index: 1;
    }
    
    .item-overlay {
      position: absolute;
      top: 0;
      left: 0;
      right: 0;
      bottom: 0;
      background: linear-gradient(to top, rgba(0,0,0,0.95) 0%, rgba(0,0,0,0.6) 50%, transparent 100%);
      z-index: 2;
    }
    
    .item-content {
      position: relative;
      z-index: 3;
    }
    
    .item-title {
      font-size: 32px;
      font-weight: bold;
      margin: 0 0 15px 0;
      line-height: 1.2;
      color: white;
      text-shadow: 2px 2px 4px rgba(0,0,0,0.5);
    }
    
    .item-price {
      font-size: 36px;
      font-weight: bold;
      color: #4ecdc4;
      margin: 10px 0;
      text-shadow: 2px 2px 4px rgba(0,0,0,0.5);
    }
    
    .item-location {
      display: flex;
      align-items: center;
      gap: 10px;
      opacity: 0.9;
      margin: 8px 0;
      font-size: 18px;
    }
    
    .item-description {
      margin: 20px 0;
      line-height: 1.6;
      font-size: 18px;
      opacity: 0.9;
      max-height: 150px;
      overflow-y: auto;
    }
    
    .item-seller {
      display: flex;
      align-items: center;
      gap: 15px;
      margin-top: 25px;
      padding-top: 15px;
      border-top: 1px solid rgba(255,255,255,0.2);
    }
    
    .seller-avatar {
      width: 50px;
      height: 50px;
      border-radius: 50%;
      background: linear-gradient(135deg, #667eea, #764ba2);
      display: flex;
      align-items: center;
      justify-content: center;
      font-weight: bold;
      font-size: 22px;
    }
    
    .item-actions {
      position: absolute;
      left: 25px;
      bottom: 100px;
      display: flex;
      flex-direction: column;
      gap: 25px;
      z-index: 3;
    }
    
    .action-btn {
      background: rgba(0,0,0,0.7);
      border: none;
      color: white;
      width: 60px;
      height: 60px;
      border-radius: 50%;
      display: flex;
      align-items: center;
      justify-content: center;
      cursor: pointer;
      transition: all 0.3s;
      font-size: 24px;
    }
    
    .action-btn:hover {
      transform: scale(1.15);
      background: rgba(78, 205, 196, 0.9);
    }
    
    /* رفع الصور */
    .image-upload {
      border: 3px dashed #4ecdc4;
      border-radius: 15px;
      padding: 40px 20px;
      text-align: center;
      cursor: pointer;
      margin: 20px 0;
      transition: all 0.3s;
    }
    
    .image-upload:hover {
      background: rgba(78, 205, 196, 0.1);
      transform: translateY(-3px);
    }
    
    .image-preview {
      display: flex;
      gap: 15px;
      flex-wrap: wrap;
      margin-top: 20px;
      justify-content: center;
    }
    
    .preview-image {
      width: 120px;
      height: 120px;
      object-fit: cover;
      border-radius: 15px;
      border: 3px solid #4ecdc4;
      transition: transform 0.3s;
    }
    
    .preview-image:hover {
      transform: scale(1.05);
    }
    
    /* الشات */
    .chat-container {
      height: 400px;
      display: flex;
      flex-direction: column;
    }
    
    .chat-messages {
      flex: 1;
      overflow-y: auto;
      padding: 20px;
      background: rgba(255, 255, 255, 0.05);
      border-radius: 15px;
      margin-bottom: 20px;
      border: 1px solid rgba(255, 255, 255, 0.1);
    }
    
    .message {
      margin-bottom: 20px;
      padding: 15px;
      background: rgba(255, 255, 255, 0.1);
      border-radius: 18px;
      max-width: 85%;
      border-right: 4px solid #4ecdc4;
    }
    
    .message-sender {
      font-weight: bold;
      color: #4ecdc4;
      margin-bottom: 8px;
      font-size: 16px;
    }
    
    .message-text {
      line-height: 1.5;
      font-size: 16px;
    }
    
    .message-time {
      font-size: 12px;
      opacity: 0.6;
      margin-top: 8px;
      text-align: left;
    }
    
    .own-message {
      background: linear-gradient(135deg, rgba(102, 126, 234, 0.3), rgba(118, 75, 162, 0.3));
      margin-right: auto;
      margin-left: 0;
      border-right: 4px solid #667eea;
    }
    
    /* البحث */
    .search-container {
      background: rgba(0,0,0,0.9);
      padding: 20px;
      margin: 20px 0;
      border-radius: 15px;
    }
    
    .search-input {
      background: rgba(255, 255, 255, 0.1);
      border: 2px solid #4ecdc4;
      color: white;
      padding: 18px;
      border-radius: 12px;
      width: 100%;
      font-size: 18px;
    }
    
    /* اللودر */
    .loader {
      border: 5px solid rgba(255, 255, 255, 0.1);
      border-top: 5px solid #4ecdc4;
      border-radius: 50%;
      width: 60px;
      height: 60px;
      animation: spin 1s linear infinite;
      margin: 50px auto;
    }
    
    @keyframes spin {
      0% { transform: rotate(0deg); }
      100% { transform: rotate(360deg); }
    }
    
    /* التبويبات */
    .tabs {
      display: flex;
      gap: 10px;
      margin-bottom: 20px;
      overflow-x: auto;
      padding: 10px 0;
    }
    
    .tab {
      padding: 15px 25px;
      background: rgba(255, 255, 255, 0.1);
      border: none;
      border-radius: 12px;
      color: white;
      cursor: pointer;
      font-size: 16px;
      font-weight: bold;
      white-space: nowrap;
      transition: all 0.3s;
    }
    
    .tab.active {
      background: linear-gradient(135deg, #667eea, #764ba2);
    }
    
    .tab:hover {
      background: rgba(255, 255, 255, 0.2);
    }
    
    /* Mobile Responsive */
    @media (max-width: 768px) {
      .container {
        margin-top: 80px;
        padding: 10px;
      }
      
      .header h1 {
        font-size: 22px;
      }
      
      .card {
        padding: 20px;
      }
      
      .item-title {
        font-size: 26px;
      }
      
      .item-price {
        font-size: 30px;
      }
      
      .item-actions {
        left: 15px;
        bottom: 80px;
      }
      
      .action-btn {
        width: 50px;
        height: 50px;
        font-size: 20px;
      }
    }
    
    /* Scrollbar */
    ::-webkit-scrollbar {
      width: 8px;
    }
    
    ::-webkit-scrollbar-track {
      background: rgba(255, 255, 255, 0.1);
      border-radius: 10px;
    }
    
    ::-webkit-scrollbar-thumb {
      background: linear-gradient(135deg, #667eea, #764ba2);
      border-radius: 10px;
    }
    
    ::-webkit-scrollbar-thumb:hover {
      background: linear-gradient(135deg, #764ba2, #667eea);
    }
  </style>
</head>
<body>

<!-- الهيدر -->
<div class="header">
  <h1>🏪 سوق إلكتروني</h1>
</div>

<!-- المحتوى الرئيسي -->
<div class="container">
  
  <!-- التبويبات -->
  <div class="tabs" id="tabs">
    <button class="tab active" onclick="showTab('auth')">🔐 تسجيل الدخول</button>
    <button class="tab" onclick="showTab('market')">🛒 بيع منتج</button>
    <button class="tab" onclick="showTab('feed')">🏠 المنتجات</button>
    <button class="tab" onclick="showTab('chat')">💬 محادثة</button>
  </div>
  
  <!-- البحث -->
  <div class="search-container" id="searchBox">
    <input type="text" class="search-input" placeholder="🔍 ابحث عن منتج..." 
           id="searchInput" oninput="searchItems()">
  </div>
  
  <!-- تسجيل الدخول -->
  <div class="card" id="authTab" style="display: block;">
    <h3>🔐 تسجيل الدخول / التسجيل</h3>
    <input type="email" id="email" placeholder="📧 البريد الإلكتروني">
    <input type="password" id="password" placeholder="🔒 كلمة المرور">
    <button onclick="login()">تسجيل الدخول</button>
    <button onclick="register()" style="background: linear-gradient(135deg, #4ecdc4, #44a08d);">إنشاء حساب جديد</button>
    <button onclick="googleLogin()" style="background: linear-gradient(135deg, #DB4437, #C13584); margin-top: 10px;">
      <span style="margin-left: 10px;">👤</span> الدخول بحساب جوجل
    </button>
  </div>
  
  <!-- بيع منتج -->
  <div class="card" id="marketTab" style="display: none;">
    <h3>🛒 بيع منتج جديد</h3>
    <input type="text" id="title" placeholder="✏️ اسم المنتج">
    <input type="number" id="price" placeholder="💰 السعر (دج)">
    <textarea id="desc" placeholder="📝 وصف المنتج" rows="4"></textarea>
    
    <div class="image-upload" onclick="document.getElementById('imageInput').click()">
      <div style="font-size: 48px;">📸</div>
      <p style="font-size: 18px; margin: 10px 0;">انقر لرفع الصور</p>
      <p style="font-size: 14px; opacity: 0.7;">يمكنك رفع حتى 5 صور</p>
    </div>
    <input type="file" id="imageInput" accept="image/*" multiple style="display: none" onchange="handleImageUpload()">
    
    <div class="image-preview" id="imagePreview"></div>
    
    <select id="location">
      <option value="">📍 اختر المدينة</option>
      <option>الجزائر العاصمة</option>
      <option>وهران</option>
      <option>قسنطينة</option>
      <option>عنابة</option>
      <option>البليدة</option>
      <option>باتنة</option>
      <option>سطيف</option>
      <option>تيزي وزو</option>
      <option>بجاية</option>
      <option>الشلف</option>
      <option>غرداية</option>
      <option>أدرار</option>
      <option>تمنراست</option>
      <option>ورقلة</option>
    </select>
    
    <button onclick="addItem()">نشر المنتج</button>
  </div>
  
  <!-- المنتجات -->
  <div id="feedTab" style="display: none;">
    <div class="feed" id="feed"></div>
  </div>
  
  <!-- المحادثة -->
  <div class="card" id="chatTab" style="display: none;">
    <h3>💬 محادثة عامة</h3>
    <div class="chat-container">
      <div class="chat-messages" id="chatMessages"></div>
      <div style="display: flex; gap: 10px;">
        <input type="text" class="search-input" id="chatInput" placeholder="اكتب رسالتك..." 
               style="flex: 1;" onkeypress="if(event.key === 'Enter') sendChatMessage()">
        <button onclick="sendChatMessage()" style="width: auto; padding: 0 25px;">إرسال</button>
      </div>
    </div>
  </div>
  
</div>

<!-- معلومات المستخدم -->
<div id="userInfo" style="display: none; position: fixed; bottom: 20px; left: 20px; background: rgba(0,0,0,0.8); padding: 15px; border-radius: 15px; z-index: 1000;">
  <div style="display: flex; align-items: center; gap: 10px;">
    <div id="userAvatar" style="width: 40px; height: 40px; border-radius: 50%; background: linear-gradient(135deg, #667eea, #764ba2); display: flex; align-items: center; justify-content: center; font-weight: bold; font-size: 20px;"></div>
    <div>
      <div id="userEmail" style="font-size: 14px;"></div>
      <button onclick="logout()" style="background: none; border: none; color: #ff6b6b; cursor: pointer; font-size: 12px; padding: 0;">تسجيل الخروج</button>
    </div>
  </div>
</div>

<script>
  // 🔥 إعدادات Firebase - ضع معلوماتك هنا
  const firebaseConfig = {
    apiKey: "ضع_مفتاح_API_هنا",
    authDomain: "ضع_نطاق_التوثيق_هنا",
    projectId: "ضع_معرف_المشروع_هنا",
    storageBucket: "ضع_سلة_التخزين_هنا",
    messagingSenderId: "ضع_معرف_المرسل_هنا",
    appId: "ضع_معرف_التطبيق_هنا"
  };

  // تهيئة Firebase
  firebase.initializeApp(firebaseConfig);
  const auth = firebase.auth();
  const db = firebase.firestore();
  const storage = firebase.storage();
  
  // حالة التطبيق
  let currentUser = null;
  let uploadedImages = [];
  
  // تهيئة التطبيق
  document.addEventListener('DOMContentLoaded', () => {
    initApp();
  });
  
  // تهيئة التطبيق
  function initApp() {
    auth.onAuthStateChanged(user => {
      if (user) {
        currentUser = user;
        showApp();
      } else {
        showAuth();
      }
    });
  }
  
  // عرض واجهة التطبيق
  function showApp() {
    showTab('feed');
    document.getElementById('userInfo').style.display = 'block';
    document.getElementById('userEmail').textContent = currentUser.email;
    document.getElementById('userAvatar').textContent = currentUser.email[0].toUpperCase();
    loadItems();
    loadChat();
  }
  
  // عرض واجهة التسجيل
  function showAuth() {
    showTab('auth');
    document.getElementById('userInfo').style.display = 'none';
  }
  
  // تغيير التبويبات
  function showTab(tabName) {
    // إخفاء جميع التبويبات
    document.querySelectorAll('.tab').forEach(tab => {
      tab.classList.remove('active');
    });
    
    document.querySelectorAll('[id$="Tab"]').forEach(tab => {
      tab.style.display = 'none';
    });
    
    // إظهار التبويب المحدد
    document.querySelector(`.tab:nth-child(${getTabIndex(tabName)})`).classList.add('active');
    document.getElementById(tabName + 'Tab').style.display = 'block';
    
    if (tabName === 'feed') {
      document.getElementById('searchBox').style.display = 'block';
    } else {
      document.getElementById('searchBox').style.display = 'none';
    }
  }
  
  function getTabIndex(tabName) {
    const tabs = ['auth', 'market', 'feed', 'chat'];
    return tabs.indexOf(tabName) + 1;
  }
  
  // تسجيل الدخول
  function login() {
    const email = document.getElementById('email').value;
    const password = document.getElementById('password').value;
    
    if (!email || !password) {
      alert('⚠️ الرجاء إدخال البريد الإلكتروني وكلمة المرور');
      return;
    }
    
    auth.signInWithEmailAndPassword(email, password)
      .then(() => {
        alert('✅ تم تسجيل الدخول بنجاح!');
      })
      .catch(error => {
        alert('❌ خطأ في تسجيل الدخول: ' + error.message);
      });
  }
  
  // التسجيل
  function register() {
    const email = document.getElementById('email').value;
    const password = document.getElementById('password').value;
    
    if (!email || !password) {
      alert('⚠️ الرجاء إدخال البريد الإلكتروني وكلمة المرور');
      return;
    }
    
    if (password.length < 6) {
      alert('⚠️ كلمة المرور يجب أن تكون 6 أحرف على الأقل');
      return;
    }
    
    auth.createUserWithEmailAndPassword(email, password)
      .then(() => {
        alert('✅ تم إنشاء الحساب بنجاح!');
      })
      .catch(error => {
        alert('❌ خطأ في التسجيل: ' + error.message);
      });
  }
  
  // تسجيل الدخول بجوجل
  function googleLogin() {
    const provider = new firebase.auth.GoogleAuthProvider();
    auth.signInWithPopup(provider)
      .catch(error => {
        alert('❌ خطأ في الدخول بجوجل: ' + error.message);
      });
  }
  
  // تسجيل الخروج
  function logout() {
    auth.signOut()
      .then(() => {
        alert('✅ تم تسجيل الخروج بنجاح');
      })
      .catch(error => {
        alert('❌ خطأ في تسجيل الخروج: ' + error.message);
      });
  }
  
  // رفع الصور
  function handleImageUpload() {
    const files = document.getElementById('imageInput').files;
    const preview = document.getElementById('imagePreview');
    preview.innerHTML = '';
    uploadedImages = [];
    
    Array.from(files).slice(0, 5).forEach(file => {
      if (file.size > 5 * 1024 * 1024) {
        alert('⚠️ الصورة كبيرة جداً: ' + file.name);
        return;
      }
      
      const reader = new FileReader();
      reader.onload = (e) => {
        const img = document.createElement('img');
        img.src = e.target.result;
        img.className = 'preview-image';
        preview.appendChild(img);
        uploadedImages.push(file);
      };
      reader.readAsDataURL(file);
    });
  }
  
  // إضافة منتج
  async function addItem() {
    const title = document.getElementById('title').value;
    const price = document.getElementById('price').value;
    const desc = document.getElementById('desc').value;
    const location = document.getElementById('location').value;
    
    if (!title || !price || !desc || !location) {
      alert('⚠️ الرجاء ملء جميع الحقول');
      return;
    }
    
    // إضافة المنتج أولاً
    const itemRef = await db.collection('items').add({
      title,
      price: parseFloat(price),
      desc,
      location,
      userId: currentUser.uid,
      userEmail: currentUser.email,
      createdAt: firebase.firestore.FieldValue.serverTimestamp(),
      images: []
    });
    
    // رفع الصور إذا وجدت
    if (uploadedImages.length > 0) {
      const imageUrls = await uploadImages(itemRef.id);
      
      // تحديث المنتج بروابط الصور
      await itemRef.update({
        images: imageUrls
      });
    }
    
    // تفريغ النموذج
    document.getElementById('title').value = '';
    document.getElementById('price').value = '';
    document.getElementById('desc').value = '';
    document.getElementById('location').value = '';
    document.getElementById('imagePreview').innerHTML = '';
    uploadedImages = [];
    
    showTab('feed');
    alert('✅ تم نشر المنتج بنجاح!');
  }
  
  // رفع الصور إلى Firebase
  async function uploadImages(itemId) {
    const imageUrls = [];
    
    for (let i = 0; i < uploadedImages.length; i++) {
      const file = uploadedImages[i];
      const fileName = `${itemId}_${Date.now()}_${i}`;
      const storageRef = storage.ref(`items/${fileName}`);
      
      try {
        const snapshot = await storageRef.put(file);
        const downloadURL = await snapshot.ref.getDownloadURL();
        imageUrls.push(downloadURL);
      } catch (error) {
        console.error('❌ خطأ في رفع الصورة:', error);
      }
    }
    
    return imageUrls;
  }
  
  // تحميل المنتجات
  function loadItems() {
    const searchTerm = document.getElementById('searchInput').value.toLowerCase();
    
    db.collection('items')
      .orderBy('createdAt', 'desc')
      .onSnapshot(snapshot => {
        const feed = document.getElementById('feed');
        feed.innerHTML = '';
        
        snapshot.forEach(doc => {
          const item = doc.data();
          
          // البحث
          if (searchTerm && 
              !item.title.toLowerCase().includes(searchTerm) && 
              !item.desc.toLowerCase().includes(searchTerm) &&
              !item.location.toLowerCase().includes(searchTerm)) {
            return;
          }
          
          const itemElement = createItemElement(item, doc.id);
          feed.appendChild(itemElement);
        });
      }, error => {
        console.error('❌ خطأ في تحميل المنتجات:', error);
      });
  }
  
  // البحث
  function searchItems() {
    loadItems();
  }
  
  // إنشاء عنصر منتج
  function createItemElement(item, itemId) {
    const div = document.createElement('div');
    div.className = 'video-item';
    
    // صورة الخلفية
    if (item.images && item.images.length > 0) {
      div.innerHTML = `<img src="${item.images[0]}" class="item-image" alt="${item.title}">`;
    }
    
    div.innerHTML += `
      <div class="item-overlay"></div>
      
      <div class="item-content">
        <h2 class="item-title">${item.title}</h2>
        <div class="item-price">${item.price.toLocaleString()} دج</div>
        <div class="item-location">
          📍 ${item.location}
        </div>
        <div class="item-description">${item.desc}</div>
        
        <div class="item-seller">
          <div class="seller-avatar">${item.userEmail[0].toUpperCase()}</div>
          <div>
            <div><strong>${item.userEmail}</strong></div>
            <small>${formatDate(item.createdAt?.toDate())}</small>
          </div>
        </div>
      </div>
      
      <div class="item-actions">
        <button class="action-btn" onclick="contactSeller('${item.userEmail}')">
          💌
        </button>
        <button class="action-btn" onclick="saveItem('${itemId}')">
          ❤️
        </button>
        <button class="action-btn" onclick="shareItem('${item.title}', '${itemId}')">
          ↗️
        </button>
      </div>
    `;
    
    return div;
  }
  
  // المحادثة
  function loadChat() {
    db.collection('chat')
      .orderBy('timestamp', 'asc')
      .limit(50)
      .onSnapshot(snapshot => {
        const messagesDiv = document.getElementById('chatMessages');
        messagesDiv.innerHTML = '';
        
        snapshot.forEach(doc => {
          const msg = doc.data();
          const messageElement = createMessageElement(msg);
          messagesDiv.appendChild(messageElement);
        });
        
        // التمرير للأسفل
        messagesDiv.scrollTop = messagesDiv.scrollHeight;
      });
  }
  
  function createMessageElement(msg) {
    const div = document.createElement('div');
    const isOwn = msg.userId === currentUser?.uid;
    
    div.className = `message ${isOwn ? 'own-message' : ''}`;
    div.innerHTML = `
      <div class="message-sender">${isOwn ? 'أنت' : msg.userEmail}</div>
      <div class="message-text">${msg.text}</div>
      <div class="message-time">${formatTime(msg.timestamp?.toDate())}</div>
    `;
    
    return div;
  }
  
  function sendChatMessage() {
    const input = document.getElementById('chatInput');
    const message = input.value.trim();
    
    if (!message || !currentUser) return;
    
    db.collection('chat').add({
      text: message,
      userId: currentUser.uid,
      userEmail: currentUser.email,
      timestamp: firebase.firestore.FieldValue.serverTimestamp()
    });
    
    input.value = '';
  }
  
  // وظائف مساعدة
  function formatDate(date) {
    if (!date) return '';
    return date.toLocaleDateString('ar-SA', {
      year: 'numeric',
      month: 'long',
      day: 'numeric'
    });
  }
  
  function formatTime(date) {
    if (!date) return '';
    return date.toLocaleTimeString('ar-SA', {
      hour: '2-digit',
      minute: '2-digit'
    });
  }
  
  function contactSeller(email) {
    const subject = `مهتم بالمنتج في سوق إلكتروني`;
    const body = `مرحباً، أنا مهتم بالمنتج الذي نشرته.`;
    window.open(`mailto:${email}?subject=${encodeURIComponent(subject)}&body=${encodeURIComponent(body)}`);
  }
  
  function saveItem(itemId) {
    if (!currentUser) return;
    
    db.collection('savedItems').add({
      userId: currentUser.uid,
      itemId: itemId,
      savedAt: firebase.firestore.FieldValue.serverTimestamp()
    });
    
    alert('💖 تم حفظ المنتج في المفضلة!');
  }
  
  function shareItem(title, itemId) {
    const url = window.location.href + '?item=' + itemId;
    const text = `شاهد هذا المنتج: ${title}`;
    
    if (navigator.share) {
      navigator.share({
        title: 'سوق إلكتروني',
        text: text,
        url: url
      });
    } else {
      navigator.clipboard.writeText(url + '\n\n' + text);
      alert('✅ تم نسخ الرابط!');
    }
  }
  
  // بدء التطبيق
  initApp();
</script>

</body>
</html>
